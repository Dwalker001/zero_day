mkdir
git init<use only if you wanna create new projects>
git config --global user.name ""
git config --global user.email ""
touch
vi <file>
git commit -m ""
git log
git diff<show differences or latest changes in files>
git rm -r --cached <file>
git rm -r --cached .
git add <file>
git add .
ls -A<list all including hidden folders>
ls -l<list files and folders with details>
rmdir
rm -rf .git
git status
Keep up!


Emacs commands(Windows)
C-w cut an entire line
M-w Copy the entire line
C-z : Stop Emacs and quit immediately without confirmation (All changes in buffer are lost).
C-g : Cencel current command and revert back from command mode
C-u : undo the last command.
C-h i : Help in Emacs- describes emacs shortcuts and commands.
C-x C-c : Save and quit.


git remote add origin https://github.com/Dwalker001/zero_day.git
git branch -M main
git push -u origin main
